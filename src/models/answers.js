// Isso é um arquivo de exemplo, crie os arquivos das models necessárias.

import {answerControl} from "../controllers/answers.js"
class ball8magic{
    constructor(answer, randomNumber){
        this._answer = answer
        this._randomNumber = randomNumber
    }

}


export {ball8magic}